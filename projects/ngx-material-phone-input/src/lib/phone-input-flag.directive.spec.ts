import { PhoneInputFlagDirective } from './phone-input-flag.directive';

describe('PhoneInputFlagDirective', () => {
  it('should create an instance', () => {
    const directive = new PhoneInputFlagDirective();
    expect(directive).toBeTruthy();
  });
});
